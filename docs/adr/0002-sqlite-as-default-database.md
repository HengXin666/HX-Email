# 默认使用 SQLite 存储

核心版继续使用 SQLite 作为默认数据库，因为目标部署形态偏个人与小团队，旧版 outlookEmailPlus 也以 SQLite 为主要持久化方式，便于本地运行、Docker 单实例部署和数据目录挂载。重写时会围绕可用邮箱、平台绑定和邮箱池重新组织 schema，但数据库选择不升级为 Postgres；如果未来出现多实例写入或更强并发需求，再通过迁移边界演进。
